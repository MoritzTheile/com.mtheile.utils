import { MenuItemLink } from 'react-admin';
import TocIcon from '@material-ui/icons/Toc';
import * as React from 'react';

export function getSubMenuMODULNAME_TOKEN(onMenuClick: () => void, open: boolean, dense: boolean) {
  return (
    <>
      {/*<MenuItemLink
        to={'/' + ftConfiguration.entityResourceName}
        primaryText={`FT - FTDataPoint`}
        leftIcon={<TocIcon />}
        onClick={onMenuClick}
        sidebarIsOpen={open}
        dense={dense}
      />*/}
    {/*<!-- CODEGENERATOR_NEEDLE_FOR_ADDING_MENU_ENTRIES (don't remove) -->*/}

    </>
  );
}
