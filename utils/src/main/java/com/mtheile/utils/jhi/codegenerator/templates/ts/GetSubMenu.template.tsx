import { MenuItemLink } from 'react-admin';
import resources from 'app/litho-ui/mydata/resources/resources';
import TocIcon from '@material-ui/icons/Toc';
import * as React from 'react';
// import { entityLIrradiationReactorResourceName } from 'app/litho-ui/mydata/resources/l-irradiation-reactor/EntityResource';
// <!-- CODEGENERATOR_NEEDLE_FOR_ADDING_IMPORTS (don't remove) -->

export function getSubMenuMODELNAME_TOKEN(onMenuClick: () => void, open: boolean, dense: boolean) {
  return (
    <>
      {/*<MenuItemLink
        to={'/' + ftConfiguration.entityResourceName}
        primaryText={`FT - FTDataPoint`}
        leftIcon={<TocIcon />}
        onClick={onMenuClick}
        sidebarIsOpen={open}
        dense={dense}
      />*/}
    {/*<!-- CODEGENERATOR_NEEDLE_FOR_ADDING_MENU_ENTRIES (don't remove) -->*/}

    </>
  );
}
